import Layout from '@theme/Layout';

export default function Changelog(): JSX.Element {
  return (
    <Layout title="Changelog" description="RickyData release notes">
      <main style={{ padding: '2rem' }}>
        <h1>Changelog</h1>
        <p>Release notes — coming soon.</p>
      </main>
    </Layout>
  );
}
